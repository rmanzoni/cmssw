#include <map>
#include <utility>
#include <vector>
#include <string>

namespace {
    struct dictionary {
        std::vector<std::vector<bool>> dummy;
        std::vector<std::map<std::string, bool>> dummy2;
        std::vector<std::vector<std::pair<std::string, float>>> dummy3;
        std::vector<int8_t> dummy4;
        std::vector<std::map<std::string, float>> dummy5;
    };
}
